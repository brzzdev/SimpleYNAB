# CI/dev tools installed with `brew bundle`. SwiftLint/SwiftFormat are pinned in
# the Mintfile and run via `mint`, so they are not brewed here.
brew "just"
brew "lefthook"
brew "mint"
brew "xcbeautify"
cask "tuist"
